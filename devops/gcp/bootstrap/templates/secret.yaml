apiVersion: v1
kind: Secret
metadata:
  labels:
    app: "{{ .Values.conf.app_name }}-bootstrap"
  annotations:
    rollme: {{ randAlphaNum 5 | quote }}
  name: "{{ .Values.conf.app_name }}-bootstrap-job-secret"
  namespace: "{{ .Release.Namespace }}"
type: Opaque
data:
  OPENID_PROVIDER_CLIENT_SECRET: "{{ .Values.secret.openid_provider_client_secret | b64enc }}"
